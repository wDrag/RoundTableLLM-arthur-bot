{
    "name": "@roundtable/master",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "main": "dist/server.js",
    "types": "dist/server.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json && tsc-alias -p tsconfig.json",
        "dev": "tsx watch src/server.ts",
        "lint": "tsc -p tsconfig.json --noEmit",
        "format": "prettier --check src",
        "test": "vitest run"
    },
    "dependencies": {
        "@fastify/cors": "^8.5.0",
        "@roundtable/core": "workspace:*",
        "@roundtable/providers": "workspace:*",
        "@roundtable/prompts": "workspace:*",
        "@roundtable/runners": "workspace:*",
        "fastify": "^4.28.1",
        "sqlite": "^5.1.1",
        "sqlite3": "^5.1.7"
    },
    "devDependencies": {
        "vitest": "^1.6.0"
    }
}